set imap_user = nwforrer@gmail.com
set imap_pass = wmibjaeuqjxgvdno

set folder = imaps://imap.gmail.com/
set spoolfile = +INBOX
mailboxes = +INBOX

set header_cache = ~/.cache/mutt
set message_cachedir = "~/.cache/mutt"

set postponed = +[Gmail]/Drafts

unset imap_passive
set imap_keepalive = 300
set mail_check = 120

# don't duplicate the sent folder automatically created by Gmail
unset record

set realname = 'Nick Forrer'
set from = nwforrer@gmail.com
set use_from = yes

set smtp_url = smtps://$imap_user:$imap_pass@smtp.gmail.com
set ssl_force_tls = yes
